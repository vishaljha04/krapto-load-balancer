services:
  - type: web
    name: krapto-load-balancer
    env: docker
    dockerfilePath: ./Dockerfile
    plan: free  # Change to "starter" or "pro" for production
    envVars:
      - key: PORT
        value: 80
    healthCheckPath: /health
    healthCheckTimeout: 10
    autoDeploy: true